<%= simple_form_for @song do |f| %>
  <%= f.input :syllables_per_line, label: "Max syllables per line" %>
  <%= f.input :lines_per_stanza, label: "Max lines per stanza" %>
  <%= f.button :submit, 'Submit', class: "btn btn-primary" %>
<% end %>
